/*
  Write a program that accepts a String as an argument.

  The function should capitalise ONLY every other letter in the string.
  
  The function should then return the converted string.
  "hello"  "HELLO" "  Hello" "he  llo"
*/

const altLetter = (str) => {
  let newStr = "";

  for (let i = 0; i <= str.length - 1; i++) {}
};

const isValid = altLetter("HELLO");
console.log(isValid);
